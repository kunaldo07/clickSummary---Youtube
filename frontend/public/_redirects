# Serve static files directly
/sitemap.xml /sitemap.xml 200
/robots.txt /robots.txt 200
/favicon.ico /favicon.ico 200
/*.png /:splat 200
/*.jpg /:splat 200
/*.json /manifest.json 200

# SPA fallback for all other routes
/* /index.html 200
